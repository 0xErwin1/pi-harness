import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/** Keeps legacy extension discovery compatible without activating review. */
export default function reviewGate(_pi: ExtensionAPI): void {}
